# Pin critical dependencies first to prevent upgrades
numpy==1.26.4
torch==2.1.0
transformers==4.35.2

# Application dependencies
fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.5.3

# ML dependencies - must be compatible with pinned versions above
# sentence-transformers 2.2.2 requires transformers>=4.6.0,<5.0.0
# We pin transformers==4.35.2 which satisfies this constraint
sentence-transformers==2.2.2

